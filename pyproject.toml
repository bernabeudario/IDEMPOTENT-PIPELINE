[project]
name = "IDEMPOTENT-PIPELINE"
version = "0.1.0"
description = "Proyecto para trabajar con un ETL idempotente."
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.0",
    "pandas>=2.3.3",
    "streamlit>=1.55.0",
]
